<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        权限策略:
        - 不申请 INTERNET 也能运行(广告跳过、手势模拟完全不依赖网络)
        - INTERNET 仅用于用户主动开启"联网更新"时,UpdateChecker 才会发起请求
        - ACCESS_NETWORK_STATE 用于判断当前是否真的连得上网
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RedGuoSkip"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            无障碍服务:核心能力来源
            注意:
            - android:canRetrieveWindowContent 必须为 true,才能读取屏幕文本判断广告
            - android:accessibilityEventTypes 订阅窗口变化、视图滚动、内容变化即可
        -->
        <service
            android:name=".service.AdSkipAccessibilityService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/ad_skip_service_config" />
        </service>

    </application>

</manifest>
